version: '3.9'
services:
  react-app:
    build:
      context: .
      dockerfile: web.Dockerfile
    volumes:
      - ./:/usr/src/app # ローカルをコンテナ内にマウント
    command: sh -c "npm start"
    ports:
      - "3000:3000"
    stdin_open: true 
